import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import Log from "../Log.vue"

describe("Log", () => {
    it ("renders properly", () => {
        const log = mount(Log)
        
    })
})