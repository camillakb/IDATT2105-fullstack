import { describe, it, expect } from 'vitest'

import { mount } from '@vue/test-utils'
import Operations from "../Operations.vue"

describe("Operations", () => {
    it ("renders properly", () => {
        const operation = mount(Operations)
        
    })
})